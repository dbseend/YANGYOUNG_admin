import React, { useState } from "react";
import styled from "styled-components";
import { createGlobalStyle } from "styled-components";

const AtTable = ({buttonText}) => {
return (
<div>
    <div>{`${buttonText}반 출석부`}</div>
    
</div>
);
};

export default AtTable;